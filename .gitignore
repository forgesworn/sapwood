node_modules/
dist/
.vite/
*.local
.DS_Store

# Local AI agent settings
.claude/

# MCP config (may contain private keys)
.mcp.json

# Playwright MCP artifacts
.playwright-mcp/

# Flasher firmware binaries (build artifacts from heartwood-esp32 — populate via save-image)
public/firmware/
