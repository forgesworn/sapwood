node_modules/
dist/
.vite/
*.local
.DS_Store

# Local AI agent settings
.claude/

# MCP config (may contain private keys)
.mcp.json

# Playwright MCP artifacts
.playwright-mcp/

# Playwright E2E artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Flasher firmware binaries are now committed (built from heartwood-esp32 via
# scripts/build-firmware.sh + espflash save-image) so they deploy with the site
# and the live flasher always serves the matching firmware.
dist-cli/
coverage/
