/// <reference types="svelte" />
/// <reference types="vite/client" />

/** App version, injected from package.json at build (see vite.config.ts). */
declare const __APP_VERSION__: string
/** Short git commit the build was cut from (see vite.config.ts). */
declare const __BUILD__: string
